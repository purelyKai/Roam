/**
 * App configuration constants
 * Storage keys, API settings, and other app-wide configuration
 */

// AsyncStorage keys
export const STORAGE_KEYS = {
  REMAINING_TIME: "@roam_remaining_time",
} as const;

// API configuration
export const API_CONFIG = {
  DEFAULT_RADIUS: 2500,
} as const;
