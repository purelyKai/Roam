// Storage keys
export const STORAGE_KEYS = {
  REMAINING_TIME: "@roam_remaining_time",
} as const;

// API configuration
export const API_CONFIG = {
  DEFAULT_RADIUS: 2500,
} as const;

// App colors (matching the design system)
export const COLORS = {
  PRIMARY: "#E20074",
  PRIMARY_LIGHT: "#ff007f",
  BACKGROUND: "#fff5fa",
  WHITE: "#ffffff",
  TEXT_PRIMARY: "#333",
  TEXT_SECONDARY: "#777",
  TEXT_MUTED: "#888",
  SUCCESS: "#4ADE80",
} as const;
