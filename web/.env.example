# API Configuration
# For local development (when running on same machine as Pi)
VITE_API_URL=http://localhost:7241

# For production (when hosted on Vercel, connecting to Pi)
# Replace with your Pi's public IP or domain
# VITE_API_URL=http://your-pi-ip:7241
